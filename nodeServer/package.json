{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is a node server in chat app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pri",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
